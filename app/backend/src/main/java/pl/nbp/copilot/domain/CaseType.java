package pl.nbp.copilot.domain;

/** Type of the service case submitted by the employee. */
public enum CaseType {
    /** Complaint (reklamacja) — customer dissatisfied with the product. */
    COMPLAINT,
    /** Return (zwrot) — customer wants to return the product. */
    RETURN
}
